# Debug-tab worked example — code

The canonical location for this example's source (adapters, skeleton, tests) is:

**`refactoring-examples/sub-team-6/debug-tab/`**

A second copy used to live here under `debug-tab-code/`. It had drifted from the canonical version (the canonical `DebugTabViewModel` implements `IDisposable`, applies a `MaxEntries` cap, and the test file covers both; the local copy did not) and has been removed to stop the two trees diverging further. Edit only the canonical path above.
