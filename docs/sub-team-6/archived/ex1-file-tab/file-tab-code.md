# File-tab worked example — code

The canonical location for this example's source (adapters, skeleton, tests, before-trace) is:

**`refactoring-examples/sub-team-6/file-tab/`**

A second copy used to live here under `file-tab-code/`. It had drifted (missing `OnDestroy` cleanup in `FileTabCompositionRoot` and `FileTabView`) and has been removed to stop the two trees diverging further. Edit only the canonical path above.

Sibling folders in this directory remain authoritative for their own artefacts:

- `file-tab-design/` — before/after class diagrams, dependency graphs, DSMs.
- `file-tab-info-docs/` — scope and narrative.
